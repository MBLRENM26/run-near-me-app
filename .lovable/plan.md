Update `src/components/site/Footer.tsx`: change the "List your event" anchor's `href` from `"#"` to `https://forms.gle/zKoor4P44UyZS92H9`, and add `target="_blank"` and `rel="noopener noreferrer"`. Since the Footer is shared across the homepage and all region pages, this single change covers every page.